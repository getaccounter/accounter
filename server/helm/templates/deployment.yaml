{{- include "app.deployment" (list . "server.deployment") -}}
{{- define "server.deployment" -}}
spec:
  template:
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: {{ .Values.image }}
          env:
            - name: PORT
              value: "{{ .Values.port }}"
            - name: POSTGRES_DB
              value: "postgres"
            - name: POSTGRES_USER
              value: "postgres"
            - name: POSTGRES_PASSWORD
              value: "postgres"
            - name: POSTGRES_URL
              value: "database"
            - name: SLACK_CLIENT_ID
              value: ""
            - name: SLACK_CLIENT_SECRET
              value: ""
          ports:
            - containerPort: {{ .Values.port }}
{{- end -}}
