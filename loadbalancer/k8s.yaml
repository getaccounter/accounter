apiVersion: v1
kind: Service
metadata:
  name: reversed-proxy
spec:
  ports:
    - port: 8080
  type: LoadBalancer
  selector:
    app: reversed-proxy
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: reversed-proxy
spec:
  selector:
    matchLabels:
      app: reversed-proxy
  template:
    metadata:
      labels:
        app: reversed-proxy
    spec:
      containers:
        - name: reversed-proxy
          image: reversed-proxy
          env:
            - name: PORT
              value: "8080"
            - name: PROTOCOL
              value: "http"
            - name: WEB_HOST
              value: "web"
            - name: WEB_PORT
              value: "3000"
            - name: SERVER_HOST
              value: "server"
            - name: SERVER_PORT
              value: "8000"
          ports:
            - containerPort: 8080