const Loading = () => {
  // temp loading comp, needs to be repalced by spinner or something
  return (
    <div className="flex justify-center items-center height h-full">
      <span>Loading...</span>
    </div>
  );
};

export default Loading;
